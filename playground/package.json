{
  "name": "nanoace-playground",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "In-browser interactive playground for nanoACE (non-core example).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "jsdom": "^29.1.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
