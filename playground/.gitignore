node_modules/
dist/
.vite/
*.log

# Weight blobs are generated by export_weights.py and are NOT committed
# (weight-hosting decision parked). Regenerate them before `npm run dev` / `npm test`.
public/models/
